{
  "name": "utility-e2e",
  "private": true,
  "type": "module",
  "description": "E2E integration tooling. Pins a stellar-sdk version that can parse protocol-28 XDR (deploy/createContract), independent of the app runtime deps.",
  "dependencies": {
    "@stellar/stellar-sdk": "14.0.0"
  }
}